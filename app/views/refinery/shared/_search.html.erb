<%= form_tag refinery.search_path, :method => 'get' do -%>
  <%= text_field_tag :query, {}, {:type => "search",
                     :placeholder => t(".search_site_for"),
                     :value => (params[:query] if params[:query])} %>
  <%= submit_tag t(".go") %>
<% end %>